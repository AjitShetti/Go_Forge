[
  "cfg[\"port\"] gives 0 both when the key is missing and when it's set to 0, so the value alone can't tell those apart. A map read can return a second value.",
  "Use the comma-ok form: if p, ok := cfg[\"port\"]; ok { return p }. Reading from a nil map is allowed and gives ok == false, so nil needs no special case."
]
